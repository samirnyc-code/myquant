# local database — regenerable from the screenshots/*.json
gamma.db

# credentials — never commit
.env

# raw screenshots / auth state
screenshots/*.png
auth_state.json

# one-time discover artifacts
discover_page.html
.discover_ready
